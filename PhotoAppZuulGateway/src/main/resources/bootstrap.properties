#This property file will be loaded before any beans are created also before the application properties are loaded.

#uri of the config server
spring.cloud.config.uri=http://localhost:8012

#name of the config server
spring.cloud.config.name=PhotoAppApiConfigServer

